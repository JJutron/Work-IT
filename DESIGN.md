# SANZERO 디자인 문서

## 브랜드 방향
- **모드**: Light Editorial / Compliance (공공형 신뢰 + 에디토리얼 타이포)
- **태그라인**: 정당한 보상, 처음부터. (푸터·문서 제목)
- **히어로 헤드라인**: 혹시, 못 받은 보상금 있으신가요?
- **마크**: 방패 안의 원(Zero)과 S 스트로크 — 산재 Zero + 보호. 헤더 워드마크는 **SANZERO** 아래 「산업재해 보상 서비스」(히어로 eyebrow 없음)
- **보드**: [`.anydesign-capture/sanzero-brandkit.png`](.anydesign-capture/sanzero-brandkit.png)

## 토큰
| Token | Value | Role |
|---|---|---|
| `ink` | `#191817` | 본문·CTA fill |
| `surface` | `#FDFDFD` | 페이지 배경 |
| `muted` | `#6e6a64` | 보조 텍스트 |
| `border` | `#CDD4DC` | hairline |
| `accent` | `#2563EB` | 링크·포커스·현재 단계. 히어로 주 버튼 fill이 아님 |
| `status.pending` | `#9cd5fe` | 대기 |
| `status.review` | `#f077af` | 심사 |
| `status.approved` | `#10B981` | 승인 |
| `status.action` | `#F59E0B` | 보완 요청 |

- 폰트: Pretendard
- Display: `clamp(2.5rem, 5vw, 4.5rem)` / weight 800 / tracking `-0.02em`
- 홈 히어로 CTA는 ink fill, `min-h-14`·`text-lg`. 테두리 링이 밖으로 퍼지는 `sz-stamp`(2.8s). `accent` `#2563EB`는 링크·포커스·진행 현황 현재 단계만. 히어로 문구는 **내 보상금 확인하기** → `/compensation/calculator`. 이어가기는 진행 현황 트랙(텍스트 링크)과 하단 바. 노무사는 최후 수단이라 헤더·히어로에 두지 않고 푸터 「전문가 상담」만 남김. 헤더 로그인은 CTA와 같은 sharp(`rounded-none`).
- 카드: 그림자 없음. 홈 진행 현황은 `rounded` 없이 hairline. 히어로와 기능 챕터 사이의 구분선은 이 카드 테두리만 쓰고, 기능 섹션에 `border-t`를 겹치지 않는다. 실무 화면은 `border-border rounded-xl`
- 적용한 2026 트렌드: 대담한 타이포(실무 히어로 `text-page`·`[0n]` 마커), 짧은 모션(입장 280ms·호버 150ms·탭 100ms, `--ease-out-quint`, `prefers-reduced-motion`), 접근성(스킵 링크·포커스 링), 점진적 다음 단계(검색·계산 다음에만 다음 액션). 홈 Next는 transform·opacity만. 카드 그림자 리프트 없음.
- 버린 트렌드: 실험 내비, 도파민 팔레트, 다크모드, 게임화, 뉴모피즘, 레트로퓨처, 맥시멀, 콜라주, 네오브루탈, 음성/챗봇 UI, 컬러 fill CTA 4종, side-tab, 이모지 버튼.
- **채택 컴프** (`.impeccable/mocks/sanzero-planned-*.png` 4장)
  - 진행 현황은 **한 줄 타임라인**(계산 → 장해등급 → 판례 → 신청서), 화면 카피는 「내 보상금, 지금 어디까지 왔나요」. 분석 내역은 다시 보기. 히어로 CTA는 「내 보상금 확인하기」.
  - 진행 현황과 기능 챕터 사이에 **산업재해 실황 스크롤**을 둔다. 고용노동부 2024년 확정 통계만 사용하며, 하루 평균 사람 단위·보험급여 대장·평균 산식의 세 장면으로 구성한다. 카드 그리드나 외부 인포그래픽 이미지는 쓰지 않는다.
    - 핵심 수치: 재해자 142,771명, 수급자 405,539명, 보험급여 7조 6,333억 원, 1인당 단순 평균 약 1,882만 원.
    - 평균액 바로 아래에 「내가 받을 보상금은 이 금액과 다릅니다」를 둔다. 제도 전체 평균(`총 지급액 ÷ 수급자 수`)이며 개인 예상액이 아니다. 기준연도·승인일 기준·고용노동부 원문 링크를 항상 함께 표시한다.
    - 실황 도입 배경은 Pexels `industry-worker-factory.jpg`(Hoang NC), 중간 전환 장면은 제공 사진 `industry-worker-warehouse.jpg`를 쓴다. 사진 위 텍스트에는 어두운 scrim을 둔다. 도입 사진에는 촬영자·원본 링크를 표시한다. 통계 도해 배경에는 사진을 깔지 않는다.
    - Next에서는 배경 사진을 스크롤에 따라 미세하게 이동하고, 핵심 수치는 0에서 최종값까지 카운트업한다. 하루 평균 390명은 `사람 모양 1개 ≈ 10명`인 39개 단위가 순차로 채워지고, 지급액 막대는 `scaleX`, 평균 산식·산정 요소는 280ms ease-out 순차 진입으로 표현한다. reduced-motion에서는 최종 수치와 완성된 차트를 즉시 보인다.
    - 실황 타이포는 핵심 수치 22–32px, 산식 18–28px, 설명 15–16px, 차트·출처 메타 12–14px을 바닥값으로 삼는다. 종이색·먹색·muted·steel hairline과 현장 사진의 안전모 주황을 유지하고, 별도 장식색을 추가하지 않는다.
  - 홈 소개는 기능 챕터 한 덩어리. 과정 번호 `01–04`는 진행 현황·계산기 스텝퍼에만. 하단 접수 대장 도해·명세 표는 두지 않음.
    - 가운데 제목 **내 조건으로 받을 금액부터 확인하세요.** / 리드: 보상금을 계산한 뒤 장해등급과 판례를 확인하고, 신청서까지 이어서 작성할 수 있습니다.
    - 챕터 3개 지그재그(4:3 실화면 루프 WebP/GIF + 동사 배지 + 기능 3줄 + 텍스트 링크). 배지 `계산` / `분석` / `신청`. 챕터 CTA는 fill이 아님. 캡처는 `scripts/capture_feature_stages.mjs`.
    - 하단 CTA 바 1곳. 히어로 주 버튼만 ink fill + 도장 링. 챕터 CTA는 fill이 아님. 캡처는 `scripts/capture_feature_stages.mjs`.
  - 홈 모션: 히어로·진행 현황은 CSS `sz-enter`(JS 전에 재생). 기능 챕터는 Framer `whileInView` 스태거. 현재 단계는 점 스케일+힌트 opacity 점멸(1.4s). 내비 밑줄 `scaleX`, CTA `scale(0.97)` 탭, 스테이지 `translateY(-3px)` 호버(그림자 없음). `MotionConfig reducedMotion="user"`. `prefers-reduced-motion`이면 점멸 없음.
    - 히어로 CTA만 fill(ink). 파란 SaaS 필은 쓰지 않음.
    - 히어로는 warm-paper 위에 **방패 프레임**(헤더 마크와 같은 SVG 실루엣, 더블 헤어라인). 카피·CTA는 프레임 안 가운데. 프레임 안에는 Zero·S 도장을 넣지 않는다(제목과 겹침). 동심원 워터마크·청사진 그리드·3D GLB·마크 해설 캡션·7:5 분할은 쓰지 않는다. 리드 「보상금」은 글자 중간에서 끊지 않는다(`word-break: keep-all`). 제목은 opacity 순차 입장, 프레임은 닫힌 패스라 꼭대기·밑점이 miter로 붙고, 위에서 아래로 좌우가 함께 드러난다. `prefers-reduced-motion`이면 둘 다 정지.
    - 방패는 보호·신뢰를 담는 브랜드 상징으로 유지하되 히어로에서는 2D 증명서 테두리로 쓴다. GLB는 `static/models`에 남겨 두되 홈에서 로드하지 않는다.
  - 판례: 좌 사이드바 + KPI 4(검색 결과/카테고리/평균 유사도/유리 판례). 유리 판례는 근로자에게 유리하게 끝난 건수(유리 O). 유리도 = 유리 O / 검색 건수.
  - 장해: 좌 사이드바 + KPI 3(가능성/등급/신뢰도) + 확률 바 + 등급 분포 + 분석 근거. 플로우 2단계. 결과 주 CTA는 판례, 보조는 이 등급으로 다시 계산.
  - 계산기: 좌 01–04 스텝퍼 + 우 폼 + 파란 다음. 화면 상단에 계산→장해등급→판례→신청 스텝퍼. 계산 다음 주 CTA는 장해등급 예측.

## XML 파일 목록

### 1. 공통 컴포넌트
- **@header.xml**: editorial 헤더 (마크+서비스 설명, 네비, 인증)
- **@footer.xml**: giant CTA + 긴급연락 한 줄
- **@dashboard.xml**: 히어로(방패 프레임) + 진행 현황 + 산업재해 실황 스크롤 + 기능 챕터 3(지그재그) + 하단 CTA. 하단 접수 대장 도해 없음.

### 2–7. 그 외 화면
로그인·신청·분석·노무사·관리자는 홈과 같은 토큰·헤더 패턴(`[01]` 마커, ink CTA, hairline 카드)을 사용합니다.
